/**
 * @fileoverview
 * 2D Point 
 */

/**
 * Point is a wrapper class to JMXPoint objects (used within the JMX engine)
 * @addon
 * @constructor
 */
function Point(x, y)
{ 
    /**
     * The X axis.
     * @type float
     */
    this.x = x;
    /**
     * The Y axis.
     * @type float
     */
    this.y = y;
    // ... 
}

